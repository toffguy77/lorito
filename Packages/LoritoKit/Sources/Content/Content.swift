// Content — loads the compiled content bundle and exposes levels/themes/cards.
// Implemented in the content-loading tasks of bootstrap-foundation.

import Domain

public enum Content {
    public static let layer = "Content"
}
